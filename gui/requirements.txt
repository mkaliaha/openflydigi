# SPDX-FileCopyrightText: 2026 Mikalai Kaliaha
#
# SPDX-License-Identifier: GPL-3.0-or-later

# GUI-only dependencies. The flydigi/ backend and everything in tools/ must
# keep working with no third-party packages at all, so nothing here may be
# imported outside gui/.
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r gui/requirements.txt
#   .venv/bin/python -m gui
#
# PySide6-Essentials rather than PySide6: it is smaller, and it leaves out the
# add-ons that are GPL-3.0-only in Qt's open-source release (Charts, Data
# Visualization, Virtual Keyboard), so they cannot be reached for by accident.
PySide6-Essentials>=6.6
